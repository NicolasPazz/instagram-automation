// user(Example).ts

// **Ejemplo de formato para el archivo user.ts**

// Reemplaza con tus propias credenciales para login y el usuario objetivo
export const loginUser = 'tu_usuario_de_login';  // Usuario para login
export const loginPass = 'tu_contraseña_de_login';  // Contraseña para login
export const targetUser = 'usuario_objetivo';  // Usuario cuyo perfil se analizará

// cambia el nombre del archivo a user.ts antes de ejecutar
